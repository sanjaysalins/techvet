Senior Mobile Engineer — Health-Tech (cross-platform with native escape hatches)
Stockholm, Sweden (hybrid, 2 days on-site)

About us
We build a wellness-monitoring app used by 1.4M EU users, with a connected-devices layer (smart scales, BLE wearables). We're hiring our 4th mobile engineer to take ownership of the cross-platform shell and the native modules for sensor-heavy paths.

The work
- ~70% Flutter (Dart 3.6, riverpod, go_router) — the consumer-facing app
- ~20% native escape hatches — Swift 6 + SwiftUI for the iOS BLE/HealthKit slice, Kotlin 2.0 + Jetpack Compose for the Android Bluetooth + HealthConnect slice
- ~10% the legacy React Native module we're winding down (don't worry — your job isn't migration, just keeping the lights on)

Must-have
- 5+ years shipping production mobile apps to App Store + Play Store
- Strong Flutter + Dart: state management (Riverpod / Bloc), platform channels for native interop, custom render objects when needed
- One of: Swift+SwiftUI or Kotlin+Jetpack Compose at senior level (we'll teach the other)
- App lifecycle / background-task expertise on at least one platform
- Crash reporting + analytics (Firebase Crashlytics, Sentry, mixpanel-style funnels)

Nice to have
- BLE / GATT protocol experience
- HealthKit (iOS) or Health Connect (Android) integration
- Expo experience (some of our internal demo apps still live on Expo SDK 53)
- Fastlane or Bitrise CI experience

Stack we use
Flutter 3.41, Dart 3.6, Swift 6, SwiftUI, Kotlin 2.0, Jetpack Compose, React Native 0.78 (legacy), Firebase (Auth + Crashlytics + Remote Config), Sentry, GitHub Actions for CI, Fastlane for store automation.

Probably not for you
- If your last 4 years were on Cordova / Ionic / PhoneGap, we'll need to recalibrate
- We don't ship games — no Unity / Unreal / native game-engine work
- Backend work is owned by a separate team — you won't write Spring Boot or Postgres on this role

Working style
Async-first (we're across Europe + Cape Town). Fortnightly retros. Two on-call shifts per quarter, business-hours only.
